# React Component Architecture Migration

## Purpose

This article explains the new architecture that future React components are to be written under, and for existing components to be rewritten under.

## Modules

For a component `MyComponent`,

### `MyComponent` (directory)

Each module for a given component should exist under

### `MyComponent.tsx`
